<Window x:Class="HeroArena.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hero Arena" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BrushBgDark}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16161A" BorderBrush="#2A2A35" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <TextBlock Text="⚔ HERO ARENA" FontSize="20" FontWeight="Black"
                           Foreground="{StaticResource BrushAccentGold}"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtWelcome" FontSize="13"
                           Foreground="{StaticResource BrushTextMuted}"
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- TabControl -->
        <TabControl Grid.Row="1" Background="{StaticResource BrushBgDark}"
                    BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="20,12"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="Transparent"
                                        BorderThickness="0,0,0,3"
                                        BorderBrush="Transparent"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush"
                                                Value="{StaticResource BrushAccentGold}"/>
                                        <Setter Property="Foreground"
                                                Value="{StaticResource BrushAccentGold}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground"
                                                Value="{StaticResource BrushTextPrimary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Onglet Héros -->
            <TabItem Header="🧙 HÉROS">
                <Frame x:Name="FrameHeroes" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- Onglet Spells -->
            <TabItem Header="✨ SPELLS">
                <Frame x:Name="FrameSpells" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- Onglet Combat -->
            <TabItem Header="⚔ COMBAT">
                <Frame x:Name="FrameCombat" NavigationUIVisibility="Hidden"/>
            </TabItem>

            <!-- Onglet Settings -->
            <TabItem Header="⚙ SETTINGS">
                <Frame x:Name="FrameSettings" NavigationUIVisibility="Hidden"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>