<Page x:Class="HeroArena.Views.SpellsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BrushBgDark}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filtre par héros -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="FILTRER PAR HÉROS :" FontSize="12" FontWeight="Bold"
                       Foreground="{StaticResource BrushTextMuted}"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox x:Name="CmbHeroes" Width="200"
                      Background="#16161A" Foreground="{StaticResource BrushTextPrimary}"
                      BorderBrush="#3A3A4A" Padding="10,6"
                      SelectionChanged="CmbHeroes_SelectionChanged"/>
        </StackPanel>

        <!-- Liste des spells -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="LstSpells">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#16161A" CornerRadius="6"
                                Padding="16" Margin="8" Width="280">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Name}" FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource BrushTextPrimary}"/>
                                </StackPanel>
                                <Border Background="#C9A84C22" CornerRadius="3"
                                        Padding="8,4" HorizontalAlignment="Left"
                                        Margin="0,0,0,8">
                                    <TextBlock FontSize="13" FontWeight="Bold"
                                               Foreground="{StaticResource BrushAccentGold}">
                                        <Run Text="{Binding Damage}"/>
                                        <Run Text=" DMG"/>
                                    </TextBlock>
                                </Border>
                                <TextBlock Text="{Binding Description}" FontSize="12"
                                           Foreground="{StaticResource BrushTextMuted}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>